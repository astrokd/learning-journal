
function forTest() {
    var result = 'start';

    for (var i = 0; i < 5; i++) {
        result = result + ' ' + i;
        console.log('inside the for loop',result);
    }
    console.log('after the for loop', result);
}

varible a equals Welcome;
